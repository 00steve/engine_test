uniform sampler2D texture1;
void main()
{
	gl_FragColor = texture2D(texture1, gl_TexCoord[0].st);
	float z = gl_FragCoord.z / gl_FragCoord.w / 300;
	//gl_FragColor = mix(mix(gl_FragColor,gl_Color),vec4(0,0,0,0),z);
	//gl_FragColor = mix(gl_FragColor,gl_Color);
	gl_FragColor = mix(gl_Color,vec4(0,0,0,0),z);
	gl_FragColor = gl_FragColor * texture2D(texture1, gl_TexCoord[0].st);
}